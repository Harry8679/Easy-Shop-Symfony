{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="min-h-screen bg-gradient-to-br from-blue-100 via-green-100 to-white py-10 px-4">
        <div class="max-w-2xl mx-auto bg-white shadow-xl rounded-2xl p-8 border border-blue-100">
            <h1 class="text-3xl font-extrabold text-blue-900 mb-6">Add Product</h1>

            {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                {{ form_errors(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}